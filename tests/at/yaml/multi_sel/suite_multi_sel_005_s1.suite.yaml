name: suite_multi_sel_005_s1_suite
app: deepin-voice-note
module: multi_sel
setup:
- action: session_start
  command: bash -lc 'exec ./tests/at/setup_voice_note_at.sh'
  wait: 6.0
teardown:
- action: session_stop
suites:
- id: suite_multi_sel_005_s1
  name: 多选状态下Ctrl取消选中单条
  description: 覆盖1676333；Shift选中三条后Ctrl+click取消其中一条，验证多选工具条仍在
  steps:
  - step_type: action
    action: session_start
    command: bash -lc './tests/at/scripts/ctrl_deselect_in_multiselect.py'
    wait: 3.0
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      name: MultipleChoicesView
  - step_type: assert
    action: assert_element
    selector:
      name: DeleteButton
  - step_type: assert
    action: assert_element
    selector:
      name: VoiceNoteMainWindow
