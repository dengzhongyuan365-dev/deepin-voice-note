name: play_voice_suite
app: deepin-voice-note
module: play_voice
setup:
- action: session_start
  command: bash -lc 'export QTWEBENGINE_REMOTE_DEBUGGING=9222; SINK=$(pactl get-default-sink
    2>/dev/null || true); if [ -n "$SINK" ]; then pactl set-default-sink "$SINK" 2>/dev/null
    || true; pactl set-default-source "$SINK.monitor" 2>/dev/null || true; fi; exec
    ./tests/at/setup_voice_note_at.sh'
  wait: 6.0
teardown:
- action: session_stop
suites:
- id: suite_play_voice_001_s1
  name: 播放暂停与Space快捷键
  description: 录制语音后验证播放/暂停按钮及 Space 快捷键切换
  steps:
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      script: "(function(){\n  const btn = document.querySelector('button[data-format=\"\
        insertVoice\"]');\n  if (!btn) throw new Error('insertVoice button not found');\n\
        \  btn.click();\n  return true;\n})()\n"
    wait: 1.0
    note: 点击 Tiptap 录音按钮
  - step_type: action
    action: session_start
    command: bash -lc 'A=tests/at/fixtures/tengwanggexu.mp3; if command -v ffplay
      >/dev/null 2>&1; then timeout 5s ffplay -nodisp -autoexit -loglevel quiet "$A";
      elif command -v paplay >/dev/null 2>&1; then timeout 5s paplay "$A"; else xdg-open
      "$A"; fi'
    wait: 5.5
    note: 播放内置音频并录制约5秒
  - step_type: action
    action: element_action
    selector:
      name: StopButton
    do: click
    wait: 2.0
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 15
      script: "(function(){\n  return new Promise(function(resolve, reject){\n   \
        \ const deadline = Date.now() + 12000;\n    function poll(){\n      const\
        \ count = document.querySelectorAll('.voiceInfoBox[data-type=\"voice-block\"\
        ]').length;\n      if (count > 0) return resolve({voiceCount: count});\n \
        \     if (Date.now() > deadline) return reject(new Error('voice block not\
        \ generated'));\n      setTimeout(poll, 300);\n    }\n    poll();\n  });\n\
        })()\n"
    wait: 0.5
    note: 等待语音块生成
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .voiceBtn
      button: left
    wait: 1.0
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .voiceBtn
      button: left
    wait: 1.0
  - step_type: action
    action: element_action
    selector:
      name: TiptapWebView
    do: focus
    wait: 0.3
  - step_type: action
    action: keyboard_press
    key: space
    wait: 0.8
  - step_type: action
    action: keyboard_press
    key: space
    wait: 0.8
  assert_steps:
  - step_type: assert
    action: assert_process_running
    process: deepin-voice-note
  - step_type: assert
    action: assert_element
    selector:
      name: TiptapWebView
- id: suite_play_voice_002_s1
  name: 右键菜单_保存复制剪切删除
  description: 录制语音后通过语音块真实右键菜单覆盖复制、剪切、粘贴、保存为MP3和删除链路
  steps:
  - step_type: action
    action: command
    command: bash -lc 'rm -rf /tmp/voice_item.mp3'
    wait: 0.2
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      script: "(function(){\n  const btn = document.querySelector('button[data-format=\"\
        insertVoice\"]');\n  if (!btn) throw new Error('insertVoice button not found');\n\
        \  btn.click();\n  return true;\n})()\n"
    wait: 1.0
    note: 点击 Tiptap 录音按钮
  - step_type: action
    action: session_start
    command: bash -lc 'A=tests/at/fixtures/tengwanggexu.mp3; if command -v ffplay >/dev/null
      2>&1; then timeout 5s ffplay -nodisp -autoexit -loglevel quiet "$A"; elif command
      -v paplay >/dev/null 2>&1; then timeout 5s paplay "$A"; else xdg-open "$A"; fi'
    wait: 5.5
    note: 异步播放内置音频作为录音源（session_start 故意不等待进程结束）
  - step_type: action
    action: element_action
    selector:
      name: StopButton
    do: click
    wait: 2.0
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 20
      script: "(function(){\n  return new Promise(function(resolve, reject){\n    const\
        \ deadline = Date.now() + 15000;\n    function poll(){\n      const count =\
        \ document.querySelectorAll('.voiceInfoBox[data-type=\"voice-block\"]').length;\n\
        \      if (count > 0) return resolve({voiceCount: count});\n      if (Date.now()\
        \ > deadline) return reject(new Error('voice block not generated'));\n     \
        \ setTimeout(poll, 300);\n    }\n    poll();\n  });\n})()\n"
    wait: 0.5
    note: 等待语音块生成
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .voiceInfoBox[data-type="voice-block"]
      button: right
    wait: 1.0
  - step_type: action
    action: command
    command: bash -lc './tests/at/scripts/click_visible_menu_item.py 复制 --timeout 8'
    wait: 0.8
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .ProseMirror p:last-child
      button: right
    wait: 1.0
    note: 复制后在正文空白段打开文本右键菜单
  - step_type: action
    action: command
    command: bash -lc './tests/at/scripts/click_visible_menu_item.py 粘贴 --timeout 8'
    wait: 1.5
    note: 通过应用右键菜单粘贴复制的语音笔记
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 8
      script: "(function(){\n  const count = document.querySelectorAll('.voiceInfoBox[data-type=\"\
        voice-block\"]').length;\n  if (count < 2) throw new Error('voice block copy/paste\
        \ did not create second block, count=' + count);\n  return {voiceCount: count};\n\
        })()\n"
    wait: 0.5
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .voiceInfoBox[data-type="voice-block"]
      button: right
    wait: 1.0
  - step_type: action
    action: command
    command: bash -lc './tests/at/scripts/click_visible_menu_item.py 剪切 --timeout 8'
    wait: 1.0
    note: 通过语音块右键菜单剪切其中一条语音笔记
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 8
      script: "(function(){\n  const count = document.querySelectorAll('.voiceInfoBox[data-type=\"\
        voice-block\"]').length;\n  if (count < 1) throw new Error('voice block missing\
        \ after cut');\n  if (count > 1) throw new Error('voice block cut did not remove\
        \ selected block, count=' + count);\n  return {voiceCount: count};\n})()\n"
    wait: 0.5
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .voiceInfoBox[data-type="voice-block"]
      button: right
    wait: 1.0
  - step_type: action
    action: command
    command: bash -lc './tests/at/scripts/click_visible_menu_item.py 保存为MP3 --timeout
      8'
    wait: 1.5
    note: 同步点击可见菜单项；不可用 session_start（异步 Popen 不等待，会导致文件对话框未弹出）
  - step_type: action
    action: file_dialog_select
    path: /tmp/
    name: voice_item.mp3
    wait: 3.0
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .voiceInfoBox[data-type="voice-block"]
      button: right
    wait: 1.0
  - step_type: action
    action: command
    command: bash -lc './tests/at/scripts/click_visible_menu_item.py 删除 --timeout 8'
    wait: 1.0
  assert_steps:
  - step_type: assert
    action: assert_process_running
    process: deepin-voice-note
  - step_type: assert
    action: assert_element
    selector:
      name: TiptapWebView
  - step_type: assert
    action: assert_file_exists
    path: /tmp/voice_item.mp3
- id: suite_play_voice_003_s1
  name: 播放中_保存流程与编辑
  description: 录制语音后播放中进入保存语音流程，取消保存后验证语音仍存在并可继续编辑正文
  steps:
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      script: "(function(){\n  const btn = document.querySelector('button[data-format=\"\
        insertVoice\"]');\n  if (!btn) throw new Error('insertVoice button not found');\n\
        \  btn.click();\n  return true;\n})()\n"
    wait: 1.0
    note: 点击 Tiptap 录音按钮
  - step_type: action
    action: session_start
    command: bash -lc 'A=tests/at/fixtures/tengwanggexu.mp3; if command -v ffplay >/dev/null
      2>&1; then timeout 5s ffplay -nodisp -autoexit -loglevel quiet "$A"; elif command
      -v paplay >/dev/null 2>&1; then timeout 5s paplay "$A"; else xdg-open "$A"; fi'
    wait: 5.5
    note: 播放内置音频并录制约5秒
  - step_type: action
    action: element_action
    selector:
      name: StopButton
    do: click
    wait: 2.0
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 20
      script: "(function(){\n  return new Promise(function(resolve, reject){\n    const\
        \ deadline = Date.now() + 15000;\n    function poll(){\n      const count =\
        \ document.querySelectorAll('.voiceInfoBox[data-type=\"voice-block\"]').length;\n\
        \      if (count > 0) return resolve({voiceCount: count});\n      if (Date.now()\
        \ > deadline) return reject(new Error('voice block not generated'));\n     \
        \ setTimeout(poll, 300);\n    }\n    poll();\n  });\n})()\n"
    wait: 0.5
    note: 等待语音块生成
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .voiceBtn
      button: left
    wait: 1.0
    note: 播放语音笔记
  - step_type: action
    action: command
    command: bash -lc './tests/at/scripts/open_note_context_menu.py --index 0 --expect
      保存语音 --click-expect --timeout 15'
    wait: 1.0
    note: 播放中打开笔记右键并点击保存语音
  - step_type: action
    action: file_dialog_cancel
    wait: 1.0
    note: 取消保存语音目录选择对话框
  - step_type: action
    action: wait
    wait: 5.0
    note: 等待语音播放自然结束
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 5
      script: "(function(){\n  const count = document.querySelectorAll('.voiceInfoBox[data-type=\"\
        voice-block\"]').length;\n  if (count < 1) throw new Error('voice block missing\
        \ after save flow');\n  return {voiceCount: count};\n})()\n"
    wait: 0.5
  - step_type: action
    action: element_action
    selector:
      name: TiptapWebView
    do: click
    wait: 0.5
  - step_type: action
    action: keyboard_type
    text: play_edit_001
    wait: 0.5
  assert_steps:
  - step_type: assert
    action: assert_process_running
    process: deepin-voice-note
  - step_type: assert
    action: assert_element
    selector:
      name: TiptapWebView
- id: suite_play_voice_004_s1
  name: 切换播放对象与右键其他记事本
  description: 录制两条语音后切换播放对象，并在播放过程中打开记事本右键菜单验证状态
  steps:
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      script: "(function(){\n  const btn = document.querySelector('button[data-format=\"\
        insertVoice\"]');\n  if (!btn) throw new Error('insertVoice button not found');\n\
        \  btn.click();\n  return true;\n})()\n"
    wait: 1.0
    note: 点击 Tiptap 录音按钮
  - step_type: action
    action: session_start
    command: bash -lc 'A=tests/at/fixtures/tengwanggexu.mp3; if command -v ffplay
      >/dev/null 2>&1; then timeout 5s ffplay -nodisp -autoexit -loglevel quiet "$A";
      elif command -v paplay >/dev/null 2>&1; then timeout 5s paplay "$A"; else xdg-open
      "$A"; fi'
    wait: 5.5
    note: 播放内置音频并录制约5秒
  - step_type: action
    action: element_action
    selector:
      name: StopButton
    do: click
    wait: 2.0
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 20
      script: "(function(){\n  return new Promise(function(resolve, reject){\n   \
        \ const deadline = Date.now() + 15000;\n    function poll(){\n      const\
        \ count = document.querySelectorAll('.voiceInfoBox[data-type=\"voice-block\"\
        ]').length;\n      if (count > 0) return resolve({voiceCount: count});\n \
        \     if (Date.now() > deadline) return reject(new Error('voice block not\
        \ generated'));\n      setTimeout(poll, 300);\n    }\n    poll();\n  });\n\
        })()\n"
    wait: 0.5
    note: 等待语音块生成
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      script: "(function(){\n  const btn = document.querySelector('button[data-format=\"\
        insertVoice\"]');\n  if (!btn) throw new Error('insertVoice button not found');\n\
        \  btn.click();\n  return true;\n})()\n"
    wait: 1.0
    note: 点击 Tiptap 录音按钮
  - step_type: action
    action: session_start
    command: bash -lc 'A=tests/at/fixtures/tengwanggexu.mp3; if command -v ffplay
      >/dev/null 2>&1; then timeout 5s ffplay -nodisp -autoexit -loglevel quiet "$A";
      elif command -v paplay >/dev/null 2>&1; then timeout 5s paplay "$A"; else xdg-open
      "$A"; fi'
    wait: 5.5
    note: 播放内置音频并录制约5秒
  - step_type: action
    action: element_action
    selector:
      name: StopButton
    do: click
    wait: 2.0
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 20
      script: "(function(){\n  return new Promise(function(resolve, reject){\n   \
        \ const deadline = Date.now() + 15000;\n    function poll(){\n      const\
        \ count = document.querySelectorAll('.voiceInfoBox[data-type=\"voice-block\"\
        ]').length;\n      if (count > 0) return resolve({voiceCount: count});\n \
        \     if (Date.now() > deadline) return reject(new Error('voice block not\
        \ generated'));\n      setTimeout(poll, 300);\n    }\n    poll();\n  });\n\
        })()\n"
    wait: 0.5
    note: 等待语音块生成
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 10
      script: "(function(){\n  const boxes = Array.from(document.querySelectorAll('.voiceInfoBox[data-type=\"\
        voice-block\"]'));\n  const buttons = boxes.map(box => box.querySelector('.voiceBtn')).filter(Boolean);\n\
        \  if (buttons.length < 2) throw new Error('need at least two voice buttons,\
        \ got ' + buttons.length);\n  buttons[0].click();\n  return {voiceButtons:\
        \ buttons.length, firstClicked: true};\n})()\n"
    wait: 1.0
    note: 播放第一条语音
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 10
      script: "(function(){\n  const boxes = Array.from(document.querySelectorAll('.voiceInfoBox[data-type=\"\
        voice-block\"]'));\n  const buttons = boxes.map(box => box.querySelector('.voiceBtn')).filter(Boolean);\n\
        \  if (buttons.length < 2) throw new Error('need at least two voice buttons,\
        \ got ' + buttons.length);\n  buttons[1].click();\n  return {voiceButtons:\
        \ buttons.length, secondClicked: true};\n})()\n"
    wait: 1.0
    note: 切换播放到第二条语音
  - step_type: action
    action: command
    command: bash -lc './tests/at/scripts/open_folder_context_menu.py --expect RenameMenuItem
      --close --timeout 10'
    wait: 1.0
    note: 播放过程中真实右键记事本条目，验证右键菜单可打开且“重命名”入口可见后关闭菜单
  assert_steps:
  - step_type: assert
    action: assert_process_running
    process: deepin-voice-note
  - step_type: assert
    action: assert_element
    selector:
      name: TiptapWebView
- id: suite_play_voice_005_s1
  name: 语音笔记显示_节点存在
  description: 录制语音后验证语音块、播放按钮和语音右键菜单入口均可通过 AT/CDP 链路定位
  steps:
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      script: "(function(){\n  const btn = document.querySelector('button[data-format=\"\
        insertVoice\"]');\n  if (!btn) throw new Error('insertVoice button not found');\n\
        \  btn.click();\n  return true;\n})()\n"
    wait: 1.0
    note: 点击 Tiptap 录音按钮
  - step_type: action
    action: session_start
    command: bash -lc 'A=tests/at/fixtures/tengwanggexu.mp3; if command -v ffplay
      >/dev/null 2>&1; then timeout 5s ffplay -nodisp -autoexit -loglevel quiet "$A";
      elif command -v paplay >/dev/null 2>&1; then timeout 5s paplay "$A"; else xdg-open
      "$A"; fi'
    wait: 5.5
    note: 播放内置音频并录制约5秒
  - step_type: action
    action: element_action
    selector:
      name: StopButton
    do: click
    wait: 2.0
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 20
      script: "(function(){\n  return new Promise(function(resolve, reject){\n   \
        \ const deadline = Date.now() + 15000;\n    function poll(){\n      const\
        \ count = document.querySelectorAll('.voiceInfoBox[data-type=\"voice-block\"\
        ]').length;\n      if (count > 0) return resolve({voiceCount: count});\n \
        \     if (Date.now() > deadline) return reject(new Error('voice block not\
        \ generated'));\n      setTimeout(poll, 300);\n    }\n    poll();\n  });\n\
        })()\n"
    wait: 0.5
    note: 等待语音块生成
  - step_type: action
    action: webengine_eval_js
    value:
      port: 9222
      target_url_contains: tiptap-editor
      timeout: 5
      script: "(function(){\n  const block = document.querySelector('.voiceInfoBox[data-type=\"\
        voice-block\"]');\n  if (!block) throw new Error('voice block not found');\n\
        \  const btn = block.querySelector('.voiceBtn');\n  if (!btn) throw new Error('voice\
        \ play button not found');\n  return {voiceBlock: true, playButton: true};\n\
        })()\n"
    wait: 0.5
  - step_type: action
    action: webengine_mouse_event
    value:
      port: 9222
      target_url_contains: tiptap-editor
      selector: .voiceInfoBox[data-type="voice-block"]
      button: right
    wait: 1.0
  - step_type: assert
    action: assert_element
    selector:
      name: VoiceContextMenu
  - step_type: action
    action: keyboard_press
    key: Escape
    wait: 0.5
  assert_steps:
  - step_type: assert
    action: assert_process_running
    process: deepin-voice-note
  - step_type: assert
    action: assert_element
    selector:
      name: TiptapWebView
  - step_type: assert
    action: assert_element
    selector:
      name: NoteItemListView
