name: _V25_2500（测试部专用）_B类解耦商店应用_语音记事本_语音记事本V25(#117769)_case_1676427_s1_suite
app: deepin-voice-note
description: ''
module: _V25_2500（测试部专用）_B类解耦商店应用_语音记事本_语音记事本V25(#117769)
tags: []
fast_fail: false
env_check: []
setup:
- action: session_start
  command: bash -lc 'exec ./tests/at/setup_voice_note_at.sh'
  wait: 6.0
teardown:
- action: session_stop
suites:
- id: case_1676427_s1
  name: 打开语音记事本
  description: ''
  tags: []
  steps:
  - step_type: action
    action: command
    command: bash -lc './tests/at/scripts/open_folder_context_menu.py --index 0 --expect DeleteFolderMenuItem --click-expect --wait-confirm --timeout 15'
    wait: 1.0
    note: dtk_context_menu 易超时；用 FolderListView 真实坐标右键并等待删除确认框
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      name: CancelButton
    note: 记事本删除确认框已弹出（同 1676419，以 CancelButton 断言）
