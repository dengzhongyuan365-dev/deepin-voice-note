name: suite_工具栏_006_s1_suite
app: deepin-voice-note
module: 工具栏
setup:
- action: session_start
  command: bash -lc 'exec ./tests/at/setup_voice_note_at.sh'
  wait: 6.0
teardown:
- action: session_stop
suites:
- id: suite_工具栏_006_s1
  name: 工具栏-关闭主界面
  description: 覆盖1676445、1635037；Alt+F4 关闭主界面并退出进程（录音中关应用的终止提示【人工】）
  steps:
  - step_type: action
    action: keyboard_hot_key
    key: alt+F4
  assert_steps:
  - step_type: assert
    action: assert_process_not_running
