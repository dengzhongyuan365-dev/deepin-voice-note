name: SP3_详情页显示优化_suite
app: deepin-voice-note
module: SP3_详情页显示优化
setup:
- action: session_start
  command: deepin-voice-note
  wait: 3.0
suites:
- id: suite_SP3_详情页显示优化_001_s1
  name: 详情页输入不同字符组合验证
  description: ''
  steps:
  - step_type: action
    action: element_action
    selector:
      name: NewNoteButton
    do: click
  - step_type: action
    action: element_action
    selector:
      name: TiptapWebView
    do: click
  - step_type: action
    action: keyboard_type
    text: 中文English123测试
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      name: TiptapWebView
  - step_type: assert
    action: assert_element
    selector:
      name: NoteContentWebView
teardown:
- action: session_stop
